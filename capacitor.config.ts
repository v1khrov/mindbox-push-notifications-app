import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.v1khrov.pushTestApp',
  appName: 'PushTestApp',
  webDir: 'dist/mindbox-push-notifications-app/browser'
};

export default config;
